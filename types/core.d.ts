// Module entry point: the same generic contracts as the public package.
export { Change, ChangeAwareCache, ChangeAwareList, ChangeReason, ChangeSet, ChangeType, IntermediateCache, ItemChange, ListChange, ListChangeReason, ObservableCache, ObservableList, Optional, RangeChange, SourceCache, SourceList, applyChanges, setObservableDecorator, snapshotChanges } from './index.js';
